# MAWJOOD - Lost & Found System

A web application for pilgrims to report and search for lost items in the Holy Cities.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies: `npm install`
2. Run the app: `npm run dev`
